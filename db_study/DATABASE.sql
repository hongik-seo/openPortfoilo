SELECT 
* 
FROM EMP

;

SELECT EMPNO , ENAME, JOB, MGR , HIREDATE, SAL, COMM,DEPTNO FROM EMP WHERE SAL>=2000 OR SAL<=3000;

SELECT ENAME
  , EMPNO 
  , SAL 
  , DEPTNO 
FROM EMP 
WHERE ENAME LIKE'%E%'
AND DEPTNO =30
AND SAL NOT BETWEEN 1000 AND 2000

;

SELECT EMPNO
  , ENAME
  , JOB
  , MGR
  , HIREDATE
  , SAL
  , COMM
  , DEPTNO
FROM EMP
WHERE COMM IS NULL 
AND (JOB ='MANAGER' OR JOB ='CLERK')
AND ENAME  NOT LIKE '_L%'

;

SELECT ENAME
     , UPPER(ENAME)
     , LOWER(ENAME)
     , INITCAP(ENAME)
  FROM EMP
;
  
SELECT *
  FROM EMP
 WHERE UPPER(ENAME) = UPPER('scott')
;
 
 
SELECT *
  FROM EMP
 WHERE UPPER(ENAME) LIKE UPPER('scott')
;
 
SELECT UPPER(ENAME)
   FROM EMP
;
   
   
SELECT ENAME
     , LENGTH(ENAME)
     , LENGTHB(ENAME)
  FROM EMP
;
    
SELECT LENGTH('스캇') 
     , LENGTHB('스캇')
  FROM DUAL
;
   
SELECT *
  FROM EMP 
 WHERE LENGTH(JOB) >6
;
 
SELECT JOB
     , SUBSTR(JOB,1,2)
     , SUBSTR(JOB,3,2)
     , SUBSTR(JOB,5)
  FROM EMP
;
  
SELECT SUBSTR(ENAME,3)
  FROM EMP
;


SELECT JOB
     , SUBSTR(JOB, -LENGTH(JOB))
     , SUBSTR(JOB, -LENGTH(JOB),2)
     , SUBSTR(JOB, -3)
  FROM EMP
;
 

SELECT INSTR('HELLO, ORACLE!','L') --문자 시작 처음부터 L을 찾아라 
     , INSTR('HELLO, ORACLE!','L',5) -- 문자 시작 5번째부터 L을 찾아라
     , INSTR('HELLO, ORACLE!','L',2,2)-- 문자 시작 2번째 부터 2번째 L을 찾아라
  FROM DUAL  
;

SELECT *
  FROM EMP 
 WHERE INSTR(ENAME,'S') >0
;

SELECT *
  FROM EMP
 WHERE ENAME LIKE'%S%'
;

-- 대체할 문자가 지정되지 않으면 삭제됨
SELECT '010-1234-5678' AS REPLACE_BEFORE
     , REPLACE ('010-1234-5678', '-' ,' ') AS REPLACE_1 
     , REPLACE ('010-1234-5678', '-' ) AS REPLACE_2 
  FROM DUAL
;     

